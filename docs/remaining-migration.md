# Оставшаяся миграция PVTables.vue

## Статус: 18% завершено (3 из 8 composables интегрировано)

### ✅ Интегрированные composables:
1. **usePVTableNavigation** (~180 строк) - Навигация по ячейкам
2. **usePVTableStyles** (~140 строк) - Стили таблицы
3. **usePVTableCellSelection** (~230 строк) - Выделение ячеек

**Итого удалено:** ~460 строк дублирующегося кода

---

## 📋 План оставшейся миграции

### Фаза 2: Простые composables (низкая сложность)

#### 1. usePVTableExpand (~240 строк)
**Приоритет:** Высокий  
**Сложность:** Средняя  
**Зависимости:** filters, dataFields

**Функции для миграции:**
- `toogleExpandRow()` - переключение раскрытия строки
- `setExpandedRow()` - установка раскрытой строки
- `delExpand()` - удаление раскрытия
- `processFieldModifier()` - обработка модификаторов полей

**Состояние:**
- `expandedRows`
- `expandedTableTreeRows`
- `subs`
- `subfilters`
- `parent_row`

---

### Фаза 3: Средние composables (средняя сложность)

#### 2. usePVTableFilters (~110 строк)
**Приоритет:** Средний  
**Сложность:** Средняя  
**Зависимости:** fields (инициализируется в onMounted)

**Проблема:** Требует рефакторинга - fields инициализируется после создания composable

**Решение:** Создать реактивную обертку или переместить инициализацию

**Функции для миграции:**
- `initFilters()` - инициализация фильтров
- `prepFilters()` - подготовка фильтров для API
- `onSetTopFilter()` - установка верхнего фильтра
- `clearFilter()` - очистка фильтров
- `onFilter()` - обработка фильтрации

---

#### 3. usePVTableData (~120 строк)
**Приоритет:** Средний  
**Сложность:** Средняя  
**Зависимости:** api, filters, fields

**Функции для миграции:**
- `loadLazyData()` - загрузка данных
- `onPage()` - пагинация
- `onSort()` - сортировка
- `refresh()` - обновление данных

**Состояние:**
- `loading`
- `lineItems`
- `totalRecords`
- `lazyParams`

---

### Фаза 4: Сложные composables (высокая сложность)

#### 4. usePVTableActions (~330 строк)
**Приоритет:** Низкий  
**Сложность:** Высокая  
**Зависимости:** api, filters, columns, notify

**Функции для миграции:**
- `defHeadAction()` - действие для заголовка
- `defRowAction()` - действие для строки
- `excelExport()` - экспорт в Excel
- `Insert()` - вставка записи
- `Insert_child()` - вставка дочерней записи
- Компиляция шаблонов действий

---

#### 5. usePVTableCRUD (~380 строк)
**Приоритет:** Низкий  
**Сложность:** Высокая  
**Зависимости:** api, filters, lineItems, notify

**Функции для миграции:**
- `onCellEditComplete()` - завершение редактирования ячейки
- `editLineItem()` - редактирование строки
- `saveLineItem()` - сохранение строки
- `openNew()` - создание новой записи
- `deleteLineItem()` - удаление строки
- `deleteSelectedLineItems()` - удаление выбранных строк
- `confirmDeleteLineItem()` - подтверждение удаления
- `confirmDeleteSelected()` - подтверждение удаления выбранных
- Modal form функции

---

## 🎯 Рекомендуемый порядок интеграции:

1. **usePVTableExpand** - независимый, хорошо изолированный
2. **usePVTableData** - базовый функционал загрузки
3. **usePVTableFilters** - после решения проблемы с fields
4. **usePVTableActions** - после фильтров и данных
5. **usePVTableCRUD** - последний, самый сложный

---

## 📊 Ожидаемые результаты после полной миграции:

- **Текущий размер:** ~2040 строк
- **После миграции:** ~860 строк (основной компонент)
- **Экономия:** ~1180 строк
- **Улучшение читаемости:** ~58%
- **Модульность:** 100%

---

## ⚠️ Важные замечания:

1. Каждый composable должен быть протестирован после интеграции
2. Сохранять обратную совместимость
3. Документировать все изменения
4. Проверять работу всех функций после каждого шага
