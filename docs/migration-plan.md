# План постепенной миграции PVTables.vue

## ✅ Выполнено

1. **Создана резервная копия** - `src/PVTables.vue.backup`
2. **Добавлены импорты composables** в начало script секции

## 📋 План дальнейшей миграции

### Этап 1: Подготовка (ВЫПОЛНЕНО)
- [x] Создать резервную копию
- [x] Добавить импорты composables

### Этап 2: Интеграция базовых composables
Заменить существующий код на вызовы composables:

#### 2.1 Фильтры (usePVTableFilters)
- [ ] Заменить `initFilters()` на вызов из composable
- [ ] Заменить `prepFilters()` на вызов из composable
- [ ] Заменить `onSetTopFilter()` на вызов из composable
- [ ] Удалить дублирующийся код

#### 2.2 Данные (usePVTableData)
- [ ] Заменить `loadLazyData()` на вызов из composable
- [ ] Заменить `onPage()` на вызов из composable
- [ ] Заменить `onSort()` на вызов из composable
- [ ] Заменить `findIndexById()` на вызов из composable
- [ ] Удалить дублирующийся код

#### 2.3 Навигация (usePVTableNavigation)
- [ ] Заменить `findCell()` на вызов из composable
- [ ] Заменить `findNextEditableColumn()` на вызов из composable
- [ ] Заменить `findPrevEditableColumn()` на вызов из composable
- [ ] Заменить `onKeyDown()` на вызов из composable
- [ ] Заменить `onTab()` на вызов из composable
- [ ] Удалить дублирующийся код

#### 2.4 Стили (usePVTableStyles)
- [ ] Заменить `getClassBody()` на вызов из composable
- [ ] Заменить `getClassTD()` на вызов из composable
- [ ] Заменить `getColumnStyle()` на вызов из composable
- [ ] Заменить `rowClass()` на вызов из composable
- [ ] Заменить `baseRowStyle()` на вызов из composable
- [ ] Заменить `toggleDarkMode()` на вызов из composable
- [ ] Заменить `toggleSettings()` на вызов из composable
- [ ] Заменить `onToggleColomns()` на вызов из composable
- [ ] Удалить дублирующийся код

### Этап 3: Интеграция сложных composables

#### 3.1 CRUD операции (usePVTableCRUD)
- [ ] Заменить `openNew()` на вызов из composable
- [ ] Заменить `editLineItem()` на вызов из composable
- [ ] Заменить `saveLineItem()` на вызов из composable
- [ ] Заменить `deleteLineItem()` на вызов из composable
- [ ] Заменить `onCellEditComplete()` на вызов из composable
- [ ] Заменить `Insert()` на вызов из composable
- [ ] Заменить `Insert_child()` на вызов из composable
- [ ] Удалить дублирующийся код

#### 3.2 Действия (usePVTableActions)
- [ ] Заменить `defHeadAction()` на вызов из composable
- [ ] Заменить `defRowAction()` на вызов из composable
- [ ] Заменить `PVTablesMenuAction()` на вызов из composable
- [ ] Заменить `showModalForm()` на вызов из composable
- [ ] Заменить `hideModalForm()` на вызов из composable
- [ ] Заменить `submitModalForm()` на вызов из composable
- [ ] Заменить `excelExport()` на вызов из composable
- [ ] Удалить дублирующийся код

#### 3.3 Раскрытие строк (usePVTableExpand)
- [ ] Заменить `toogleExpandRow()` на вызов из composable
- [ ] Заменить `setExpandedRow()` на вызов из composable
- [ ] Заменить `delExpand()` на вызов из composable
- [ ] Удалить дублирующийся код

#### 3.4 Выделение ячеек (usePVTableCellSelection)
- [ ] Заменить `toggleCellSelectionMode()` на вызов из composable
- [ ] Заменить `getCellData()` на вызов из composable
- [ ] Заменить `onCellMouseDown()` на вызов из composable
- [ ] Заменить `onCellMouseEnter()` на вызов из composable
- [ ] Заменить `handleCellCopy()` на вызов из composable
- [ ] Удалить дублирующийся код

### Этап 4: Финальная очистка
- [ ] Удалить все дублирующиеся функции
- [ ] Проверить, что все переменные используются из composables
- [ ] Убедиться, что template использует правильные ссылки
- [ ] Оптимизировать импорты

### Этап 5: Тестирование
- [ ] Проверить работу фильтров
- [ ] Проверить загрузку данных
- [ ] Проверить CRUD операции
- [ ] Проверить навигацию по ячейкам
- [ ] Проверить выделение ячеек
- [ ] Проверить раскрытие строк
- [ ] Проверить все действия
- [ ] Проверить стили

## 🎯 Текущий статус

**Этап:** 1 из 5 (Подготовка)  
**Прогресс:** 20%

## 💡 Рекомендации

Из-за большого размера файла (~2500 строк), рекомендуется:

1. **Делать миграцию поэтапно** - по одному composable за раз
2. **Тестировать после каждого этапа** - убедиться, что всё работает
3. **Сохранять промежуточные версии** - на случай отката
4. **Не спешить** - качество важнее скорости

## 🔄 Следующий шаг

Начать с **Этапа 2.1** - интеграция usePVTableFilters, так как это самый простой composable с минимальными зависимостями.

## ⚠️ Важно

- Резервная копия находится в `src/PVTables.vue.backup`
- При возникновении проблем можно откатиться к резервной копии
- Все composables уже созданы и готовы к использованию
- Template не требует изменений, так как использует те же имена методов
