# Структура рефакторинга PVTables.vue

## ✅ Созданные Composables

### 1. `usePVTableFilters.js`
**Назначение:** Управление фильтрами таблицы
- `initFilters()` - Инициализация фильтров
- `prepFilters()` - Подготовка фильтров для API
- `onSetTopFilter()` - Обработка верхних фильтров

### 2. `usePVTableData.js`
**Назначение:** Загрузка и управление данными
- `loadLazyData()` - Загрузка данных с сервера
- `onPage()` - Обработка пагинации
- `onSort()` - Обработка сортировки
- `findIndexById()` - Поиск элемента по ID

### 3. `usePVTableCRUD.js`
**Назначение:** CRUD операции
- `openNew()` - Создание новой записи
- `editLineItem()` - Редактирование записи
- `saveLineItem()` - Сохранение записи
- `deleteLineItem()` - Удаление записи
- `onCellEditComplete()` - Редактирование ячейки
- `Insert()` / `Insert_child()` - Вставка записей

### 4. `usePVTableActions.js`
**Назначение:** Управление действиями таблицы
- `compileActionTemplate()` - Компиляция шаблонов действий
- `defHeadAction()` - Действие для заголовка
- `defRowAction()` - Действие для строки
- `showModalForm()` / `hideModalForm()` - Модальные формы
- `excelExport()` - Экспорт в Excel

### 5. `usePVTableExpand.js`
**Назначение:** Раскрытие строк (подтаблицы/подвкладки)
- `toogleExpandRow()` - Переключение раскрытия строки дерева
- `setExpandedRow()` - Установка раскрытия для подтаблиц
- `processFieldModifier()` - Обработка модификаторов полей

### 6. `usePVTableCellSelection.js`
**Назначение:** Режим выделения ячеек
- `toggleCellSelectionMode()` - Переключение режима
- `getCellData()` - Получение данных ячейки
- `onCellMouseDown()` / `onCellMouseEnter()` - Обработка выделения
- `handleCellCopy()` - Копирование данных

### 7. `usePVTableNavigation.js`
**Назначение:** Навигация по ячейкам (Tab/Enter)
- `findNextEditableColumn()` - Поиск следующей ячейки
- `findPrevEditableColumn()` - Поиск предыдущей ячейки
- `onKeyDown()` - Обработка клавиш
- `moveCell()` - Перемещение фокуса

### 8. `usePVTableStyles.js`
**Назначение:** Стили таблицы
- `getClassBody()` - Класс для тела ячейки
- `getClassTD()` - Класс для TD
- `getColumnStyle()` - Стиль колонки
- `rowClass()` / `baseRowStyle()` - Стили строк
- `toggleDarkMode()` - Переключение темы

## 📋 Следующие шаги

### Этап 1: Создание UI компонентов (опционально)
Можно создать отдельные компоненты для UI частей:
- `PVTableToolbar.vue` - Панель инструментов
- `PVTableDialogs.vue` - Все диалоги
- `PVTableDataView.vue` - Основная таблица с колонками

### Этап 2: Рефакторинг основного файла
Интегрировать все composables в `PVTables.vue`:
1. Импортировать все composables
2. Заменить существующую логику на вызовы composables
3. Убрать дублирующийся код
4. Протестировать функциональность

## 🎯 Преимущества новой структуры

1. **Модульность** - Каждый composable отвечает за свою область
2. **Переиспользование** - Composables можно использовать в других компонентах
3. **Тестируемость** - Легче писать unit-тесты
4. **Читаемость** - Код разбит на логические блоки
5. **Поддержка** - Проще находить и исправлять ошибки

## 📝 Примечания

- Все composables используют Composition API
- Сохранена обратная совместимость с существующим API
- Добавлена подробная JSDoc документация
- Учтены все edge cases из оригинального кода

## 🔄 Статус

- [x] Создать composables для логики фильтров
- [x] Создать composables для загрузки данных
- [x] Создать composables для CRUD операций
- [x] Создать composables для действий (actions)
- [x] Создать composables для работы с раскрытием строк
- [x] Создать composables для выделения ячеек
- [x] Создать composables для навигации
- [x] Создать composables для стилей
- [ ] Создать компонент PVTableToolbar (опционально)
- [ ] Создать компонент PVTableDialogs (опционально)
- [ ] Создать компонент PVTableDataView (опционально)
- [ ] Рефакторинг основного PVTables.vue
- [ ] Тестирование всех модулей

## 💡 Рекомендации по использованию

### Вариант 1: Постепенная миграция
Можно постепенно заменять части кода в `PVTables.vue` на composables, тестируя каждый шаг.

### Вариант 2: Создание новой версии
Создать `PVTablesRefactored.vue` с использованием всех composables, затем заменить старую версию после тестирования.

### Вариант 3: Гибридный подход
Использовать composables в текущем файле, но оставить UI в одном компоненте без разбивки на подкомпоненты.

## 🔍 Что дальше?

Следующий шаг - интеграция composables в основной файл `PVTables.vue`. Это можно сделать:
1. Импортировав все composables
2. Вызвав их в `setup()` с нужными параметрами
3. Заменив старую логику на методы из composables
4. Удалив дублирующийся код

Хотите продолжить с рефакторингом основного файла?
